set icons true
set shell cmd

cmd mkdir ${{
  printf "Directory Name: "
  read ans
  mkdir $ans
}}

map <enter> quit
map p paste
map x cut
map y copy

map md mkdir

map gd cd ~/.config
map gs cd ~/side
map gw cd ~/work
